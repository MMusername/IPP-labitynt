#ifndef GET_DATA_H
#define GET_DATA_H

#include <stdbool.h>

extern void get_data(size_t **a, size_t **b, size_t **c, unsigned char **d,
        size_t *n, size_t *dimensions_product, short *error);

#endif
